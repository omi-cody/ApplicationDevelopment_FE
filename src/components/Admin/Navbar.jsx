export default function Navbar() {
    return (
        <header className="admin-navbar">
            <div className="admin-navbar-title">Admin Workspace</div>
            <div className="admin-navbar-actions">
                <span>Milestone 1 UI</span>
                <span className="admin-avatar" aria-label="Current admin">AD</span>
            </div>
        </header>
    );
}
